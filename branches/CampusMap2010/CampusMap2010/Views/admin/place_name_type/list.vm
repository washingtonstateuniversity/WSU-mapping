#if($place_name_types)
	<table width="50%" border="0" class="pagination" style="float:right;margin-right:5px;margin-top:7px;">
	  <tr>
		<td>Showing $place_name_types.FirstItemIndex - $place_name_types.LastItemIndex of $place_name_types.TotalItems</td>
		<td align="right">
		  #if($place_name_types.HasPrevious) $PaginationHelper.CreatePageLink( 1, "first", null, "%{target='place_name_types'}" ) #end
		  #if(!$place_name_types.HasPrevious) first #end
		  #if($place_name_types.HasPrevious) | $PaginationHelper.CreatePageLink( $place_name_types.PreviousIndex, "prev", null, "%{target='place_name_types'}" ) #end
		  #if(!$place_name_types.HasPrevious) | prev #end
		  #if($place_name_types.HasNext) | $PaginationHelper.CreatePageLink( $place_name_types.NextIndex, "next", null, "%{target='place_name_types'}" ) #end
		  #if(!$place_name_types.HasNext) | next #end
		  #if($place_name_types.HasNext) | $PaginationHelper.CreatePageLink( $place_name_types.LastIndex, "last", null, "%{target='place_name_types'}" ) #end
		  #if(!$place_name_types.HasNext) | last #end
		</td>
	  </tr>
	</table>
	<table  width="100%" class="ui-widget ui-widget-content">
	  <thead>
		<tr class="ui-widget-header ">
		<th>Name</th>
		<th style="width:75px;">&nbsp;</th>
		</tr>
	  </thead>
	  <tbody>

		#set ($c=0)
		#foreach($type in $place_name_types)
		<tr class="#if($c%2>0) even #else odd #end #if($type.adminRead==false) ui-state-highlight #end ">
		  <td><a href="edit_name_type.castle?id=$!{type.Id}" title="Edit" class="name">
				$type.type
			  </a></td>
		  <td>
			<span style='font-size:0.7em;'>
			  <a href="edit_name_type.castle?id=$!{type.Id}" title="Edit" class="buttons ui-button ui-widget ui-state-default ui-corner-all ui-button-text-only">
				<span class="ui-icon ui-icon-pencil"></span>
			  </a>
			  <a href="delete_name_type.castle?id=$!{type.Id}" title="Delete" class="buttons ui-button ui-widget ui-state-default ui-corner-all ui-button-text-only">
				<span class="ui-icon ui-icon-trash"></span>
			  </a>
			</span>
		  </td>
		</tr>
		#set ($c=$c+1)
		#end
	  </tbody>
	</table>
	<table width="50%" border="0"  class="pagination">
	  <tr>
		<td>Showing $place_name_types.FirstItemIndex - $place_name_types.LastItemIndex of $place_name_types.TotalItems</td>
		<td align="right">
		  #if($place_name_types.HasPrevious) $PaginationHelper.CreatePageLink( 1, "first", null, "%{target='place_name_types'}" ) #end
		  #if(!$place_name_types.HasPrevious) first #end
		  #if($place_name_types.HasPrevious) | $PaginationHelper.CreatePageLink( $place_name_types.PreviousIndex, "prev", null, "%{target='place_name_types'}" ) #end
		  #if(!$place_name_types.HasPrevious) | prev #end
		  #if($place_name_types.HasNext) | $PaginationHelper.CreatePageLink( $place_name_types.NextIndex, "next", null, "%{target='place_name_types'}" ) #end
		  #if(!$place_name_types.HasNext) | next #end
		  #if($place_name_types.HasNext) | $PaginationHelper.CreatePageLink( $place_name_types.LastIndex, "last", null, "%{target='place_name_types'}" ) #end
		  #if(!$place_name_types.HasNext) | last #end
		</td>
	  </tr>
	</table>
#else
	<h1>There are currently no name types</h1>
#end